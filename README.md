# UEIK Releases Registry

This repository acts as the master registry for the plugin marketplace.

## Structure
- `community-plugins.json`: The central registry file containing all verified community plugins and pointers to official sub-registries.
